from django.urls import path,include

from . import views
urlpatterns = [
#     path("post/<int:pk>/save", views.PostSaveView.as_view(), name="post_save"),
#     path("post/count", views.PostCountView.as_view(), name="post_cnt"),
#     path("tag/suggest", views.TagSuggestView.as_view(), name="tag_suggest"),
]

